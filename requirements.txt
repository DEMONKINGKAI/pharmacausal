pandas>=2.2
pyarrow>=16.0
requests>=2.31
tqdm>=4.66
numpy>=1.26
pytest>=7.4.0

# causal discovery
causal-learn>=0.1.3.8

# Graph viz ended up as a self-contained HTML/SVG artifact (reports/) rather
# than a networkx/matplotlib render, since the deliverable is a shareable
# web page, not a static image — networkx/pgmpy/matplotlib are not used by
# any script in src/ and are intentionally left out of this file.
